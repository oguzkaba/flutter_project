import 'package:flutter/material.dart';

class PortraitAlert extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child:Text('Yalnızca yatay modda kullanılabilir..!')
      ),
    );
  }
}